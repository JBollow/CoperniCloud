---
language: node_js 
node_js:
  - "8"
cache:
  directories:
    - "node_modules"

language: python
python:
  - "3.6"
  - "nightly"

before_install:
  - sudo apt-get -qq update -y
  - sudo apt-get install libgdal-dev -y

install:
  - pip install gdal bottle numpy
  - . install.sh

script:
  - . start.sh
